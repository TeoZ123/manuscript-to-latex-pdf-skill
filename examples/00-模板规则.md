# 模板规则

这是公开仓库中的极简示例，不代表任何学校或期刊模板。

## 编译方式

- 使用 `latexmk -xelatex main.tex`。

## 文档结构

- `main.tex` 为入口。
- 正文章节放在 `contents/`。
- 图片放在 `images/`。

## 图表规则

- 图题置于图下。
- 表题置于表上。
- 资料来源单独成行。
- 表格按模板规则转换，复杂表格需要人工复核。

## 引用与参考文献

- 示例使用编号式参考文献。
- 不自动补造缺失文献。
